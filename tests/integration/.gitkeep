# Integration Tests

This directory will contain integration tests for:
- REST API endpoints (auth, leaderboard, user profiles)
- WebSocket event flows (game events, real-time sync)
- Database operations

Requires running server instance.
